#include "main.h"

/**
 * mul - multiplies two integers
 * @a - first number to multiply
 * @b - second number to multiply
 * Return: result of a and b multiplication
 */

int mul(int a, int b)
{
	return(a * b);
}
