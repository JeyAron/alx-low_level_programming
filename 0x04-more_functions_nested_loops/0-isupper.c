#include "main.h"

/**
 * _isupper - check letter if upper
 * @x : number to be ckecked
 * Return: 1 for upper or 0 for any else
 */

int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
